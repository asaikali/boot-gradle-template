server:
  port: ${PORT:8080}

spring:
  datasource:
    url: jdbc:postgresql://localhost:45432/snapcoach?user=postgres&password=password
    driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none

